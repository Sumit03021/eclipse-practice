package abstraction;

public class demo {
// encapsulation---- hiding from unauthorised access

// abstraction----hiding implementations ---achieved by abstract class, and interface.
	
// abstract class-----
// ----------abstract methods-----
//1. abstract method cannot make as the objects.
//2. abstract class compulsory but abstract method may be or may not be abstract---which does not have own body in functions.
//3. if inheritance then override the body function else abstract class will be extended.
	
// interface---- achieve the 100% abtraction
// class--- concrete 
// abstract---concrete or abstract function
// interface--- only abstract methods,

// 1. interface--- inherits by extends(iti) and (cti)implements
// 2. interface just use void fun();---show as public abstract void fun();
// 3. diamond case inheritance----class B implements A,C{ }
	
//interface---100% abstraction can achieve, method--always public abstract,
// variable--- public static final (always will be constant), constructor function (absent),multiple inheritance.

// abstract class --0 to 100%abstract achieve , methods can be change--public ,private.
}// variable not , present constructor functions (that delay process),multiple inheritance

// interface cannot be extends/implements to class but reverse possible.
// class first extends class then implements the interface(ordering)
// c1 extends c2 implements i1.







